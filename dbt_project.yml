name: 'carmen_sandiego'
version: '1.0.0'
config-version: 2

profile: 'carmen_sandiego'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target"]